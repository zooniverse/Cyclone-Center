html
  background: $blue url("./images/noise.png")
  background: url("./images/noise.png"), -moz-radial-gradient(circle farthest-side, lighten($blue, 20%), $blue) fixed
  background: url("./images/noise.png"), -webkit-radial-gradient(circle farthest-side, lighten($blue, 20%), $blue) fixed
  background: url("./images/noise.png"), radial-gradient(circle farthest-side, lighten($blue, 20%), $blue) fixed
  border-top: 5px solid #000
  box-sizing: border-box
  color: #fff
  font: 14px $sans-serif
  min-height: 100%

body
  margin: 0

  hr
    border: 0
    border-top: 1px solid rgba(#fff, 0.5)
    margin: 2em 0

  h1
    font-size: 40px
    font-weight: 300
    margin: 0.25em 0

  h2
    font-weight: 300

  a
    border-bottom: 1px solid
    color: inherit
    text-decoration: none

    &:hover
      color: yellow

  & > nav
    margin: 0 auto
    width: 1000px

    a
      color: inherit
      border-bottom: 0
      display: inline-block
      padding: 15px 10px

      &.logo
        font-size: 20px

  & > .page
    & > .page-nav
      display: none

    & > .page-subpages > .page
      display: none

      &.active
        display: block
